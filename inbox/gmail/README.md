# Gmail Intake

This folder is reserved for future Gmail intake.

Do not store Gmail credentials, OAuth tokens, or raw mailbox exports containing sensitive material unless they are intentionally staged for local review.
