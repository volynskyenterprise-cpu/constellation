# Configuration

This folder contains declarative configuration for Constellation.

Configuration should remain model-agnostic. Agents and workflows should ask for capabilities; provider files may map those capabilities to Codex, GPT, Claude, Gemini, local models, or future systems.
