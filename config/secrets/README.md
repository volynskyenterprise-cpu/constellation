# Config Secrets

Place local-only credential and token files here when enabling optional connectors.

This directory is ignored by Git except for this README. Do not commit credentials, OAuth tokens, API keys, or downloaded secret material.
