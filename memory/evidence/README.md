# Evidence Memory

This folder stores deterministic `EvidenceItem` records created by Constellation workflows.

Evidence records are file-based JSON documents. They are runtime memory artifacts, not source documents and not external retrieval results.

The Evidence Engine currently supports deterministic extraction from markdown and text inputs only.
