# Knowledge Graph Memory

This folder stores the deterministic file-based Constellation Knowledge Graph.

`graph.json` is generated by explicit graph build commands and is ignored as runtime memory. The graph is built from workflow runs, evidence records, source files, and artifacts.

No external graph database, embeddings, vector store, web retrieval, or LLM inference is used.
