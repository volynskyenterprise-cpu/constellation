# Workflows

This folder contains workflow definitions.

Workflows define how expert agents collaborate. They should specify the objective, participating agents, ordered steps, expected artifacts, approval gates, validation criteria, and memory updates.

Workflow definitions are intentionally declarative. They should not assume a specific model provider or orchestration engine.
